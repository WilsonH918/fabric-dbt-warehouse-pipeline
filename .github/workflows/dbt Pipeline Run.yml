name: dbt Pipeline Run

on:
  schedule:
    - cron: "0 5 * * *"   # daily at 05:00 UTC
  workflow_dispatch:

jobs:
  dbt-prod:
    runs-on: ubuntu-latest

    env:
      FABRIC_HOST: ${{ secrets.FABRIC_HOST }}
      FABRIC_PORT: ${{ secrets.FABRIC_PORT }}
      FABRIC_TENANT_ID: ${{ secrets.FABRIC_TENANT_ID }}
      FABRIC_CLIENT_ID: ${{ secrets.FABRIC_CLIENT_ID }}
      FABRIC_CLIENT_SECRET: ${{ secrets.FABRIC_CLIENT_SECRET }}
      FABRIC_SOURCE_DATABASE: ${{ secrets.FABRIC_SOURCE_DATABASE }}
      FABRIC_SOURCE_SCHEMA: ${{ secrets.FABRIC_SOURCE_SCHEMA }}
      FABRIC_TARGET_DATABASE: ${{ secrets.FABRIC_TARGET_DATABASE }}
      FABRIC_TARGET_SCHEMA: ${{ secrets.FABRIC_TARGET_SCHEMA }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - run: pip install dbt-core dbt-fabric
      
      - name: Install ODBC Driver 17 for SQL Server
        run: |
          sudo apt-get update
          sudo apt-get install -y curl apt-transport-https gnupg
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
          curl https://packages.microsoft.com/config/ubuntu/22.04/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17

      - run: dbt deps
      - run: dbt run
      - run: dbt test
